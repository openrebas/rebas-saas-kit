# Flows — Hello World Onboarding

1) Open /welcome → shows greeting + CTA
2) Click "Create Sample Project" → POST /projects/sample → success → route /dashboard?sample=1
3) Failure → show retry with error message
