# Contributing

- Use Issues → PRs flow. Small, focused PRs.
- Follow PR template; reference ADRs for significant decisions.
- Keep API in sync with docs/api/openapi.yaml.
- Tests before merge; preview should build.
