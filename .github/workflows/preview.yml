name: Preview
on:
  pull_request:

jobs:
  build-preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build (placeholder)
        run: |
          echo "Build your app here (Docker or static)"
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: preview
          path: dist/**
        if: ${{ hashFiles('dist/**') != '' }}
